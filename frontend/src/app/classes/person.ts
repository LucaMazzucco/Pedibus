export class Person {

    name: string;
    surname: string;
    isPresent: boolean;

    constructor(name: string, surname: string, isPresent: boolean) {
        this.name = name;
        this.surname = surname;
        this.isPresent = isPresent;
    }
}
